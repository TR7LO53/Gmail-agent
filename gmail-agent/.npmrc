# Your machine is behind a TLS-inspecting proxy. Make Node trust the Windows
# certificate store (where the proxy's root CA lives) for every npm script
# (auth, try, mcp, tests). Without this, network calls fail with
# UNABLE_TO_VERIFY_LEAF_SIGNATURE.
node-options=--use-system-ca
